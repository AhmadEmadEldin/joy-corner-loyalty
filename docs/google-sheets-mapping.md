# Google Sheets Mapping

The authoritative ten-tab mapping is in `REBUILT_WORKBOOK_SCHEMA.md` and
`server/sheets/schema.ts`. Legacy-to-canonical conversion is implemented in
`server/workbookMigration.ts`; exceptions and verified totals are recorded in
`MIGRATION_EXCEPTIONS.md` and `DATA_RECONCILIATION.md`.
