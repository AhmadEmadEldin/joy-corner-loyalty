# Google Sheets Migration Plan

Superseded by `MIGRATION_PLAN.md`. The current plan creates a verified backup,
migrates only a separate working copy to ten canonical tabs, reconciles live
reads, and leaves production configuration unchanged until owner approval.
