-- Role defaults and business settings are seeded by migrations because they
-- are required in every environment. Run `npm run seed:supabase-menu` after
-- linking to load the versioned Joy Corner menu JSON idempotently.

